# Added post-verification: no dependency version file existed for this
# project despite results depending on specific library versions.
# Versions below are what was actually used to produce this paper's
# results (local Apple Silicon runs, verified via `import <pkg>;
# print(pkg.__version__)`); the Kaggle-only real-feature-extraction run
# (code/03_real_feature_leakage_test.py) additionally installed
# gptqmodel at runtime inside the script itself (no local pin verified).
numpy==2.4.3
scipy==1.15.1
scikit-learn==1.6.1
torch==2.11.0
transformers==5.2.0
datasets==4.8.5
gptqmodel  # installed at runtime on Kaggle (code/03_real_feature_leakage_test.py); no local pin verified
